import Gameboard from "./Gameboard";

const Player = (type) => {
  const mType = type;
  const mGameboard = Gameboard();

  const getType = () => {};

  const getBoard = () => {};

  return {
    getType,
    getBoard,
  };
};